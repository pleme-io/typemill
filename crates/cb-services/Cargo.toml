[package]
name = "cb-services"
edition = "2021"
license.workspace = true

[dependencies]
codebuddy-foundation = { path = "../codebuddy-foundation" }
cb-ast = { path = "../cb-ast" }
cb-lsp = { path = "../cb-lsp" }
cb-plugin-api = { path = "../cb-plugin-api" }
anyhow = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
dashmap = { workspace = true }
uuid = { workspace = true }
lsp-types = "0.97"
tempfile = "3.0"
ignore = "0.4"
globset = "0.4"
toml_edit = "0.23"
pathdiff = "0.2"
chrono = "0.4"
cargo_metadata = "0.18"  # For dependency graph analysis
petgraph = { workspace = true }  # For dependency graph traversal
# NOTE: cb-lang-rust retained ONLY for Rust-specific module path utilities
# (compute_module_path_from_file, find_crate_name_from_cargo_toml)
# Used by reference_updater/detectors/rust.rs for cross-crate move detection
cb-lang-rust = { workspace = true }
sha2 = "0.10"
urlencoding = "2.1"
codebuddy-plugin-system = { path = "../codebuddy-plugin-system" }
codebuddy-config = { path = "../codebuddy-config" }

[features]
default = []
mcp-proxy = ["codebuddy-foundation/mcp-proxy", "codebuddy-config/mcp-proxy", "codebuddy-plugin-system/mcp-proxy"]

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
futures-util = "0.3"
chrono = "0.4"
tokio = { workspace = true }
# Language plugins needed for test discovery via inventory
cb-lang-typescript = { workspace = true }
cb-lang-markdown = { workspace = true }
cb-lang-toml = { path = "../cb-lang-toml" }
cb-lang-yaml = { path = "../cb-lang-yaml" }
