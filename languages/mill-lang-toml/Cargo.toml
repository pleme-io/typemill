[package]
name = "mill-lang-toml"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
mill-plugin-api = { path = "../../crates/mill-plugin-api" }
async-trait = "0.1"
toml_edit = "0.23"
tracing = "0.1"
serde_json = "1.0"
# fancy-regex for comment updates (supports lookahead/lookbehind)
fancy-regex = "0.13"

[dev-dependencies]
tempfile = "3.8"
tokio = { version = "1.0", features = ["full"] }
mill-test-support = { path = "../../crates/mill-test-support" }
