# search_code

Search for symbols across the workspace.

## Parameters
```json
{
  "query": "MyFunction",
  "kind": "function",
  "limit": 50,
  "offset": 0
}
```

## Examples
```json
{ "name": "search_code", "arguments": { "query": "Config", "limit": 10 } }
```
